# Optional Vite env for local (non-Docker) frontend development.
# Copy to `.env.local` if you run `pnpm frontend:dev` against a host backend.

# Nest listens on PORT from apps/backend/.env (default 3000).
VITE_API_PROXY_TARGET=http://localhost:3000
